---
"@workspace/web": patch
"@workspace/config": patch
---

Every prompt run now links out to the engine that produced it, so a visibility number can be checked against what the AI says right now, and the dashboard opens with four measured headline figures.
